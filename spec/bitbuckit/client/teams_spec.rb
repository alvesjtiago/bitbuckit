RSpec.describe Bitbuckit::Client::Teams do
  before do
    @client = Bitbuckit::Client.new
  end

  describe ".teams" do
    # it "returns all teams for the authenticated user" do
    #   teams = @client.teams
    #   expect(teams).to be_kind_of Array
    # end
  end
end
